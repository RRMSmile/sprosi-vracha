name: 🚀 Deploy Sprosivracha Frontend to Beget + SEO Audit + Telegram Notify

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Build & Deploy via SSH
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Проверка репозитория
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Node.js окружение
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3️⃣ Кеш npm
      - name: Setup npm cache
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      # 4️⃣ Установка зависимостей
      - name: Install dependencies
        run: npm ci

      # 5️⃣ Сборка Next.js (статический экспорт)
      - name: Build Next.js static site
        run: npm run build && npm run export

      # 6️⃣ Проверка SSH подключения к Beget
      - name: Check SSH connection to Beget
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.BEGET_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh -o StrictHostKeyChecking=no ${{ secrets.BEGET_USER }}@${{ secrets.BEGET_HOST }} "echo ✅ SSH connection OK"

      # 7️⃣ Оптимизация статики
      - name: Optimize static output
        run: |
          npm i -g minify
          find out -type f -name "*.js" -o -name "*.css" | while read f; do
            minify "$f" > "$f.min" && mv "$f.min" "$f"
          done

      # 8️⃣ Деплой на Beget
      - name: Deploy to Beget via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.BEGET_HOST }}
          username: ${{ secrets.BEGET_USER }}
          key: ${{ secrets.BEGET_SSH_KEY }}
          source: "out/*"
          target: "/home/e/${{ secrets.BEGET_USER }}/data/www/sprosi-vracha.com"

      # 9️⃣ Проверка доступности сайта
      - name: Verify website is online
        run: |
          curl -I https://sprosi-vracha.com

      # 🔟 Lighthouse SEO-аудит
      - name: Run Lighthouse audit
        if: always()
        continue-on-error: true
        uses: treosh/lighthouse-ci-action@v10
        with:
          urls: |
            https://sprosi-vracha.com
          uploadArtifacts: false
          temporaryPublicStorage: true

      # 1️⃣1️⃣ Сохранение отчёта Lighthouse
      - name: Save Lighthouse report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lighthouse-report
          path: .lighthouseci/

      # 1️⃣2️⃣ Очистка SSH ключей (безопасность)
      - name: Cleanup SSH key
        if: always()
        run: rm -rf ~/.ssh

      # 1️⃣3️⃣ Telegram-уведомление о результате
      - name: Notify Telegram
        if: always()
        run: |
          STATUS="${{ job.status }}"
          if [ "$STATUS" == "success" ]; then
            MSG="✅ *Deploy completed for* [sprosi-vracha.com](https://sprosi-vracha.com)%0A\
🔹 Branch: \`${{ github.ref_name }}\`%0A\
🔹 Commit: \`${{ github.sha }}\`%0A\
🕒 $(date '+%Y-%m-%d %H:%M:%S %Z')"
          else
            MSG="❌ *Ошибка деплоя!*%0AПроверь логи GitHub Actions.%0A\
🔹 Branch: \`${{ github.ref_name }}\`%0A\
🔹 Commit: \`${{ github.sha }}\`"
          fi
          curl -s -X POST https://api.telegram.org/bot${{ secrets.SSL_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.SSL_BOT_CHAT_ID }} \
            -d parse_mode="Markdown" \
            -d text="$MSG"

